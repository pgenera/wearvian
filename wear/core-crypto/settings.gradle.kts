// Standalone settings so this module can be built and tested in isolation
// (e.g. `gradle test` here) without the Android SDK. The Android app pulls it in
// as a composite build via `includeBuild("core-crypto")`.
rootProject.name = "core-crypto"
